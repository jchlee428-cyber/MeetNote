# AI 음성 회의록 앱 개발 프롬프트

## 1. 프로젝트 목표

휴대폰에서 회의 내용을 녹음하면 음성 파일을 서버로 전송하고, Whisper 계열의 음성인식 API를 이용하여 한국어 텍스트로 변환한 후, AI가 자동으로 전문적인 회의록을 작성해 주는 웹앱을 개발한다.

주요 사용자는 교회, 비영리단체, 소규모 기업, 마을공동체 등의 회의 참가자이다.

특히 다음과 같은 회의에 최적화한다.

- 당회
- 제직회
- 장로회
- 위원회
- 목장/셀 모임
- 중보기도 모임
- 비영리단체 회의
- 사업 회의
- 주민회의

---

# 2. 핵심 사용자 흐름

사용자는 휴대폰으로 웹앱에 접속한다.

### STEP 1. 회의 시작

사용자가 `회의 녹음 시작` 버튼을 누른다.

화면에는 다음을 표시한다.

- 녹음 시간
- 녹음 상태
- 일시정지
- 다시 시작
- 녹음 종료

녹음 중에는 현재 녹음 시간을 실시간으로 표시한다.

예:

`● 녹음 중 00:23:41`

---

# 3. 녹음 기능

브라우저의 MediaRecorder API를 사용하여 휴대폰 마이크로 음성을 녹음한다.

지원 형식은 브라우저 호환성을 고려하여 구현한다.

가능하면:

- audio/webm
- audio/mp4
- audio/mpeg
- audio/wav

등을 지원한다.

사용자가 녹음을 종료하면 다음 화면을 보여준다.

### 녹음 완료

- 녹음 시간
- 파일 크기
- 녹음 날짜
- 회의 제목 입력
- 회의 장소 입력
- 참석자 입력
- `음성 전사 시작` 버튼

---

# 4. Whisper 음성인식

녹음 파일을 서버 API로 업로드한다.

중요한 보안 원칙:

**OpenAI API Key를 브라우저에 노출하지 않는다.**

API Key는 반드시 서버 환경변수에 저장한다.

예:

`OPENAI_API_KEY`

프론트엔드에서 직접 OpenAI API를 호출하지 말고 다음과 같은 서버 API를 만든다.

`POST /api/transcribe`

서버가 녹음 파일을 받아 음성전사 API를 호출한다.

음성 언어는 기본값을 한국어로 설정한다.

가능하면 다음 정보를 함께 관리한다.

- 원본 음성파일
- 전사된 한국어 텍스트
- 생성 날짜
- 회의 ID
- 처리 상태

---

# 5. 음성전사 진행 화면

음성전사 중에는 진행 상태를 사용자에게 명확하게 보여준다.

예:

`음성을 분석하고 있습니다...`

단계별 표시:

1. 녹음 파일 업로드
2. 음성 분석
3. 한국어 텍스트 변환
4. AI 회의록 작성
5. 완료

처리 중에는 중복 요청이 발생하지 않도록 버튼을 비활성화한다.

---

# 6. 한국어 원문 화면

Whisper 음성전사가 완료되면 먼저 원문을 보여준다.

제목:

`한국어 음성 전사 내용`

사용자가 다음 작업을 할 수 있도록 한다.

- 원문 확인
- 수정
- 복사
- 전체 선택
- AI 회의록 생성

원문은 가능한 한 음성의 실제 내용을 보존한다.

AI가 임의로 내용을 추가하지 않는다.

---

# 7. AI 회의록 생성

전사된 한국어 텍스트를 AI에게 전달하여 회의록을 작성한다.

회의록은 다음 구조로 만든다.

## 회의록

### ① 회의 기본정보

- 회의명
- 일시
- 장소
- 참석자
- 회의 목적

### ② 주요 안건

각 안건을 번호로 구분한다.

예:

1. 예배 일정 변경
2. 행사 준비
3. 예산안 검토
4. 신규 사업 논의

### ③ 주요 논의 내용

각 안건별로 참석자들의 주요 의견을 정리한다.

불필요한 반복이나 잡담은 제거한다.

### ④ 결정사항

회의에서 실제로 결정된 사항만 정리한다.

중요:

**회의에서 결정되지 않은 내용을 AI가 결정사항으로 만들어서는 안 된다.**

### ⑤ 미결사항

결론이 나지 않은 내용은 별도로 표시한다.

### ⑥ 찬반 또는 주요 의견

의견 차이가 있었던 경우 양쪽의 의견을 공정하게 정리한다.

### ⑦ 실행사항(Action Items)

다음 형식으로 정리한다.

| 업무 | 담당자 | 기한 | 상태 |
|---|---|---|---|
| 행사 장소 확인 | 홍길동 | 9월 15일 | 예정 |
| 안내문 작성 | 김철수 | 9월 18일 | 예정 |

담당자나 기한이 음성 내용에 없으면 임의로 생성하지 않는다.

`미정`으로 표시한다.

### ⑧ 다음 회의

다음 회의 날짜나 장소가 언급되었다면 정리한다.

언급되지 않았다면 `미정`으로 표시한다.

---

# 8. 회의록 AI 작성 원칙

AI에게 다음 원칙을 반드시 적용한다.

1. 원문에 없는 사실을 만들어내지 않는다.
2. 결정되지 않은 내용을 결정사항으로 작성하지 않는다.
3. 참석자의 의견을 왜곡하지 않는다.
4. 개인에 대한 평가나 감정을 임의로 추가하지 않는다.
5. 불분명한 내용은 추측하지 않는다.
6. 불확실한 내용은 `[확인 필요]`로 표시한다.
7. 반복되는 발언은 핵심 내용 중심으로 정리한다.
8. 욕설이나 불필요한 사적인 대화는 회의록 목적에 맞게 정리한다.
9. 중요한 숫자, 날짜, 금액, 이름은 가능한 한 원문을 유지한다.
10. 회의록은 객관적이고 중립적인 문체로 작성한다.

---

# 9. 회의록 수정 기능

AI가 작성한 회의록을 사용자가 직접 수정할 수 있도록 한다.

각 영역을 편집 가능하게 한다.

- 회의 제목
- 참석자
- 안건
- 논의 내용
- 결정사항
- 실행사항
- 다음 회의

`저장` 버튼을 제공한다.

---

# 10. 저장 기능

회의가 끝난 후 다음 정보를 하나의 회의 기록으로 저장한다.

```text
meeting_id
meeting_title
meeting_date
meeting_location
participants
audio_file
transcript
minutes
created_at
updated_at
```

초기 버전에서는 간단한 데이터베이스를 사용한다.

추천:

- Supabase

필요한 테이블:

`meetings`

필드:

- id
- title
- meeting_date
- location
- participants
- audio_url
- transcript
- minutes
- created_at
- updated_at

---

# 11. 회의 목록

메인 화면에 과거 회의 목록을 보여준다.

예:

### 최근 회의

- 2026.09.05 장로회 회의
- 2026.09.03 예배위원회 회의
- 2026.08.30 중보기도팀 회의
- 2026.08.28 비영리단체 운영회의

각 회의를 클릭하면 상세 화면으로 이동한다.

검색 기능도 제공한다.

검색 대상:

- 회의 제목
- 날짜
- 참석자
- 회의록 내용
- 전사 내용

---

# 12. 회의록 다운로드

회의록 상세 화면에서 다음 기능을 제공한다.

- 복사
- 인쇄
- PDF 다운로드
- Word 다운로드

PDF와 Word 문서는 사람이 실제 회의록으로 사용할 수 있도록 깔끔한 문서 형식으로 만든다.

---

# 13. 모바일 UI

모바일 사용을 최우선으로 설계한다.

특히 스마트폰에서 다음 버튼이 크고 명확해야 한다.

`🎙 회의 녹음 시작`

녹음 화면은 최대한 단순하게 만든다.

### 메인 화면

```text
AI 회의록

음성으로 회의록을 자동으로 만들어보세요.

[ 🎙 회의 녹음 시작 ]

최근 회의

2026.09.05 장로회 회의
2026.09.03 예배위원회
2026.08.30 중보기도팀
```

---

# 14. 긴 회의 대응

30분~2시간 이상의 긴 회의도 고려한다.

대용량 음성파일을 한 번에 처리하다가 실패하지 않도록 한다.

가능하면 음성을 일정한 길이로 분할하여 처리하는 구조를 고려한다.

예:

```text
2시간 회의
↓
10~15분 단위 분할
↓
각 음성파일 전사
↓
전사 결과 합치기
↓
전체 회의록 생성
```

단, 최종 회의록은 전체 회의 내용을 종합해서 작성해야 한다.

---

# 15. 오류 처리

다음 오류 상황을 반드시 처리한다.

### 마이크 권한 거부

사용자에게:

`마이크 사용 권한이 필요합니다. 휴대폰의 브라우저 설정에서 마이크 권한을 허용해주세요.`

### 인터넷 연결 실패

`인터넷 연결을 확인한 후 다시 시도해주세요.`

### 파일 업로드 실패

`음성파일 업로드에 실패했습니다. 다시 시도해주세요.`

### 음성전사 실패

`음성을 텍스트로 변환하지 못했습니다. 잠시 후 다시 시도해주세요.`

### AI 회의록 생성 실패

전사된 원문은 보존하고 회의록 생성만 다시 실행할 수 있도록 한다.

---

# 16. 개인정보 및 보안

회의 내용에는 개인정보와 민감한 내용이 포함될 수 있으므로 보안을 중요하게 설계한다.

필수 원칙:

- API Key를 클라이언트에 노출하지 않는다.
- 서버 환경변수 사용
- 사용자별 데이터 접근 제한
- 다른 사용자의 회의록을 볼 수 없도록 한다.
- 음성파일 접근 권한을 제한한다.
- 삭제 기능 제공
- 사용자가 원할 경우 음성 원본을 삭제할 수 있도록 한다.

---

# 17. AI 프롬프트

회의록 생성 AI에는 다음과 같은 시스템 지침을 사용한다.

```text
당신은 전문적인 한국어 회의록 작성 AI입니다.

사용자가 제공한 음성 전사 텍스트만을 근거로 회의록을 작성하십시오.

절대로 원문에 없는 사실을 만들어내지 마십시오.

회의에서 실제로 결정된 사항과 단순한 의견을 명확하게 구분하십시오.

결정되지 않은 내용은 결정사항으로 작성하지 마십시오.

담당자와 기한이 명확하게 언급되지 않았다면 임의로 만들지 말고 "미정"으로 표시하십시오.

의견이 서로 다른 경우 어느 한쪽에 유리하게 표현하지 말고 중립적으로 정리하십시오.

불명확하거나 잘 들리지 않는 내용은 추측하지 말고 "[확인 필요]"라고 표시하십시오.

반복되는 발언과 불필요한 잡담은 제거하되 중요한 맥락은 유지하십시오.

회의록은 다음 순서로 작성하십시오.

1. 회의 기본정보
2. 주요 안건
3. 주요 논의 내용
4. 결정사항
5. 미결사항
6. 주요 의견
7. 실행사항
8. 다음 회의

문체는 공식적이고 객관적인 한국어를 사용하십시오.
```

---

# 18. 기술 스택

다음 기술을 우선 사용한다.

### Frontend

- Next.js
- TypeScript
- Tailwind CSS
- Responsive Design

### Backend

- Next.js API Routes 또는 Server Actions

### Database

- Supabase

### Authentication

필요한 경우 Supabase Auth를 사용한다.

### Speech-to-Text

OpenAI의 음성 전사 API를 사용한다.

API Key는 서버에서만 사용한다.

### AI 회의록

OpenAI 또는 Google Gemini API를 사용할 수 있도록 구조를 모듈화한다.

---

# 19. API 구조

다음 API를 구현한다.

```text
POST /api/transcribe
```

기능:

음성파일 → 한국어 텍스트

```text
POST /api/generate-minutes
```

기능:

한국어 전사 텍스트 → AI 회의록

```text
POST /api/meetings
```

기능:

회의 저장

```text
GET /api/meetings
```

기능:

회의 목록 조회

```text
GET /api/meetings/[id]
```

기능:

특정 회의 조회

```text
PUT /api/meetings/[id]
```

기능:

회의록 수정

```text
DELETE /api/meetings/[id]
```

기능:

회의 삭제

---

# 20. 디자인 방향

디자인은 복잡하지 않고 신뢰감을 주는 전문적인 스타일로 만든다.

핵심 키워드:

- 깨끗함
- 신뢰감
- 간결함
- 큰 버튼
- 모바일 최적화
- 한국어 중심
- 고령 사용자도 쉽게 사용할 수 있는 UI

특히 60대 이상의 사용자도 쉽게 사용할 수 있도록 글자 크기와 버튼 크기를 충분히 크게 만든다.

---

# 21. 추가 기능

가능하면 다음 기능을 확장할 수 있도록 구조를 설계한다.

### 화자 구분

가능하다면:

`화자 1`
`화자 2`
`화자 3`

형태로 발언자를 구분한다.

추후 사용자가 실제 이름을 연결할 수 있도록 한다.

### AI 검색

사용자가 다음과 같이 질문할 수 있도록 한다.

- "지난달 회의에서 예산에 대해 뭐라고 했지?"
- "지난 3개월 동안 결정된 사항을 보여줘."
- "김철수 집사에게 맡겨진 업무는?"
- "다음 회의에서 확인해야 할 사항은?"

AI는 저장된 회의록을 검색하여 답변한다.

---

# 22. 최종 목표

이 앱은 단순한 음성 녹음 앱이 아니다.

다음 과정을 자동화하는 **AI 회의 비서**를 목표로 한다.

```text
휴대폰 녹음
      ↓
음성 파일
      ↓
Whisper 음성인식
      ↓
한국어 전사
      ↓
AI 분석
      ↓
회의록 자동 작성
      ↓
결정사항 추출
      ↓
실행사항 추출
      ↓
저장
      ↓
검색
      ↓
PDF / Word 출력
```

---

# 23. 개발 시 우선순위

처음부터 모든 기능을 한꺼번에 구현하지 말고 다음 순서로 개발한다.

### Phase 1 — MVP

1. 휴대폰 마이크 녹음
2. 녹음 파일 업로드
3. 음성전사 API 연결
4. 한국어 텍스트 표시
5. AI 회의록 생성
6. 회의록 화면 표시

### Phase 2

7. Supabase 저장
8. 회의 목록
9. 회의 검색
10. 회의록 수정
11. 삭제

### Phase 3

12. PDF 다운로드
13. Word 다운로드
14. 긴 회의 처리
15. 음성 원본 관리

### Phase 4

16. 화자 구분
17. AI 회의 검색
18. 실행사항 관리
19. 담당자별 업무 관리
20. 교회/단체별 관리자 기능

---

## Antigravity 작업 지시

위 요구사항을 바탕으로 실제 실행 가능한 웹앱을 생성하십시오.

먼저 MVP를 완성하고 실행 테스트하십시오.

특히 다음 네 가지를 먼저 반드시 정상 작동시키십시오.

**① 휴대폰 녹음  
② 음성파일 업로드  
③ 한국어 음성전사  
④ AI 회의록 자동 생성**

모든 API Key는 환경변수로 처리하고 클라이언트 코드에 노출하지 마십시오.

각 단계가 정상 작동하는지 확인하면서 개발하십시오.

오류가 발생하면 원인을 분석하고 직접 수정한 후 다시 테스트하십시오.

최종적으로 스마트폰에서 실제 회의 녹음을 하고, 한국어 회의록까지 자동으로 생성되는 상태를 목표로 하십시오.